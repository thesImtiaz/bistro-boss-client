

const Button = ({button}) => {
  return (
    <div>
      <button className="btn btn-outline border-0 border-b-4 mt-8 text-lg font-medium font-inter uppercase">{button}</button>
    </div>
  );
};

export default Button;